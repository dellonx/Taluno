# 📷 Diretrizes para Imagens do Projeto Taluno

Esta pasta armazena todos os ativos visuais estáticos do projeto.

## ⚠️ Regras para adicionar novas imagens

Para manter a performance e organização, siga estas regras antes de subir um arquivo:

### 1. Nomenclatura (Padrão Snake Case)
Use letras minúsculas e *underline* para separar palavras. Não use espaços ou acentos.
* ✅ **Correto:** `logo_senac_branco.png`, `banner_aluno_feliz.jpg`
* ❌ **Errado:** `Logo Senac (1).png`, `foto.do.banner.jpg`

### 2. Organização
Quando adicionar imagens de um mesmo contexto, crie uma subpasta:
* `/img/icones/`
* `/img/banners/`
* `/img/avatares/`

### 3. Formatos Permitidos
* **Ícones:** Use `.svg` (preferencial) ou `.png` (fundo transparente).
* **Fotos:** Use `.jpg` ou `.webp` (mais leve).